import { SearchByParentPipe } from './search-by-parent.pipe';

describe('SearchByParentPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchByParentPipe();
    expect(pipe).toBeTruthy();
  });
});
