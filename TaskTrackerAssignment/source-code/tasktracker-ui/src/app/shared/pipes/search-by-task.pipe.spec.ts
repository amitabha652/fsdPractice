import { SearchByTaskPipe } from './search-by-task.pipe';

describe('SearchByTaskPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchByTaskPipe();
    expect(pipe).toBeTruthy();
  });
});
